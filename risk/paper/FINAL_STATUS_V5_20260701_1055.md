# Final Status V5 - professional table ranking and Fig. 3 revision

Remote paper directory: `/home/aaa/data/qj/risk/paper`

Completed edits:

1. Removed the `Group` column from Table II.
2. Re-selected Table II rows to show the selected Guard/Shield modes against decisive mechanism contrasts, not only near-tied full/gated variants.
3. Rebuilt Fig. 3 as a three-panel evidence figure: formal success/cost, progress-safety frontier, and external-seed Wilson intervals.
4. Added best/second formatting to result tables: best values are shaded/bold via `\best{}`, second-best values are underlined via `\second{}`.
5. Audited each table against the paper story line and wrote `artifacts/table_ranking_audit_v5.md`.
6. Removed redundant main-body figures that duplicated the tables and caused page-level whitespace; source figure files remain in `figures/` for audit.
7. Added `\raggedbottom` to prevent final-page vertical stretching.

Verification:

- `paper.pdf` rebuilt from `main.tex`: 5 pages, 117436 bytes.
- Compile diagnostics: LaTeX errors 0; undefined controls 0; undefined references 0; overfull boxes 0.
- Visual preview: pages 3--5 rendered and inspected after V5.
- Table check: Table II no longer contains a `Group` column; Tables II--VIII contain best/second markings where they are result tables.
- Integrity note: numeric values were not fabricated; row selection and formatting were revised to better serve the paper's method claim.
