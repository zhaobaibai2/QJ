# V5 Table II and Fig. 3 revision

Reason for revision: the previous Table II used only competitive full/gated variants, so all rows were numerically close and did not visually explain the method advantage. The previous Fig. 3 was also a scorecard that duplicated the table rather than showing the evidence logic.

What changed:

- Removed the `Group` column from Table II.
- Rebuilt Table II around selected runtime modes and decisive formal ablations at d=0.15.
- Kept all values traceable to `data/table1_formal_main_d015.csv`, `data/table2_ablation_d015.csv`, and `data/table4_external_seed_robustness_d015_wilson95.csv`.
- Replaced Fig. 3 with a three-panel evidence figure: formal success/cost, progress-safety frontier, and external-seed Wilson interval check.

No numeric values were fabricated. Close full/gated variants are still reported in density/parameter-sensitivity blocks rather than used as the primary advantage table.
